<form  class="form-inline" action="{{=URL('console', 'get_data_seriess')}}">
	<div class="input-prepend input-append success">
		<span class="add-on success" for="inputWarning">Frontends</span>
		<select class="" name="frontend" id="select_frontend">
		{{for type in data_types: }}
			<option value="{{=type[0]}}">{{=type[0]}}</option>
		{{pass}}
		</select>
		<input type='hidden' name='frontend' value='{{=frontend}}'></input>
		<button id='frontend_frontend' type="submit" class="update btn btn-success">Add</button>
	</div>
	</div>
</form>


